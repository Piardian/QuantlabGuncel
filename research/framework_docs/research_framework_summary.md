# Research Framework Summary

Default toggles preserve production behaviour. Baseline validation compares a
production-style call with a framework-default call at trade and portfolio level.
Experiments are reproducible from their immutable config and generated manifest.

The framework creates controls; it does not run ablations, optimize parameters,
or recommend trading changes.
