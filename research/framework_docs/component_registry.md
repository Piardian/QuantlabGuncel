# Component Registry

The executable component registry is defined in `research/component_registry.py`.
It lists only decisions implemented by `leadership_expansion_v1`, their execution
stage, and the exact research-only semantics when disabled.

No volume filter exists in the production entry logic, so it is not registered.
