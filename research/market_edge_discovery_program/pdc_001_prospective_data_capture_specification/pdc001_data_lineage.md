# Data Lineage

Required lineage:

```text
source
  -> raw artifact
  -> normalized artifact
  -> security master
  -> universe snapshot
  -> future alpha input
```

Current status:

`PARTIAL`

Reason:

The architecture is documented, but source access is unresolved.
