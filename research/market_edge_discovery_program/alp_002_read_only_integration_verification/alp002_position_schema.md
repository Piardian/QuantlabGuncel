# ALP-002 Position Schema

Endpoint: `GET /v2/positions`

Result: `PASS`

Observed position count: `0`.

Zero positions are acceptable in ALP-002 because the purpose is endpoint/schema handling. No position was created to test this endpoint.

If positions exist later, schema capture must be repeated without creating orders during read-only verification.

